# RAG-prototype-
I built this RAG Prototype just for study purpose it uses local ollama model to embed slides and textbooks into a vector database, then does a semantic search based on the passed query and retrives relevant information and the model answers based on the retrived information and refrecences the page number which it used as reference.
